fun fib @n
  if Math.le(@n, 1)
    return 1
  .

  return Math.add(fib(Math.sub(@n, 1)), fib(Math.sub(@n, 2)))
.

fun main
  [1, 2, 3, 4]
    > map($, fib)
    > map($, @i -> String.concat(@i, '\n'))
    > each($, IO.print)
.
